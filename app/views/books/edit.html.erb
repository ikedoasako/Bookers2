<div class="container px-5 px-sm-0" >
  <div class= "row">
    <%= render 'book', user: @user %>
  <div class="col-md-8 offset-md-1">
    <h1>Books</h1>
    <table class="table">
      <thead>
        <tr>
          <th>Title</th>
          <th>Opinion</th>
          <td><%= image_tag @book.user.get_profile_image(70,70) %><br>
              <%= link_to @book.user.name, user_path(@book.id) %></td>
        </tr>
      </thead>
    </table>

